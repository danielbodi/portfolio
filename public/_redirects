# Legacy project routes (renamed to /work/*)
/projects/sopra /work/sopra-banking 301
/projects/* /work/:splat 301

# Redirect all HTTP traffic to HTTPS
http://* https://:splat 301

# Client-side routing is handled by the Worker assets configuration.
